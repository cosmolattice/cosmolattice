Below we present all the currently implemented functions in the different algebra. Note that if unfortunately the function/operation you want is not implemented, you can do it very easily yourself by opening a similar function from the library, copy it and use it as a template for your missing function/operation.

### Scalar Algebra

The files are located in `src/include/TempLat/lattice/algebra/operators/`.

| Function name            | Operation                                         | File                    | Notes                                                                                                                                                                                                                      |
| ------------------------ | ------------------------------------------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| operator+($\phi, \chi$)  | $\phi + \chi$                                     | add.h                   | Addition of `ZeroType` is simplified to no addition.                                                                                                                                                                       |
| operator-($\phi, \chi$)  | $\phi-\chi$                                       | subtract.h              | Simplifies to no subtraction if one of the input is of `ZeroType`. Simplifies $\phi-(-\chi)$ to $\phi+\chi$. Handles `HalfType` and `OneType` appropriately.                                                               |
| operator-($\phi$)        | $-\phi$                                           | unaryminus.h            | `-ZeroType` returns `ZeroType`. Simplifies $--\phi$ to $\phi$.                                                                                                                                                             |
| operator*($\phi, \chi$)  | $\phi \chi$                                       | multiply.h              | Multiplication by `ZeroType` returns `ZeroType` and multiplication by `OneType` is simplified away.                                                                                                                        |
| operator/($\phi, \chi$)  | $\phi/\chi$                                       | divide.h                | `ZeroType$/\chi$ is simplified to `ZeroType` and $\chi/$OneType` is simplified to `OneType`.                                                                                                                             |
| safeDivide($\phi,\chi$)  | $\phi/\chi$                                       | divide.h                | Same as `operator/` except that if $\chi$ is a field, it checks point by point that the divisor is not too small and discards the division if this is so. Useful when dividing by some fields which fluctuates around $0$. |
| abs($\phi$)              | $                                                 | \phi                    | $                                                                                                                                                                                                                          | absolutevalue.h |                                                                                                 |
| arg($\phi, \chi$)        | $\mathrm{arg}(\phi+ i \chi)$                      | arg.h                   |                                                                                                                                                                                                                            |
| asinh($\phi$)            | $\mathrm{asinh}(\phi)$                            | asinh.h                 |                                                                                                                                                                                                                            |
| conj($\phi$)             | $\phi^*$                                          | complexconjugate.h      | Useful for the Fourier modes, as they are complex even for a scalar field.                                                                                                                                                 |
| cosh($\phi$)             | $\mathrm{cosh}(\phi)$                             | cosh.h                  | `cosh`(ZeroType) is simplified to OneType.                                                                                                                                                                                 |
| cos($\phi$)              | $\mathrm{cos}(\phi)$                              | cosine.h                | `cos`(ZeroType) is simplified to OneType.                                                                                                                                                                                  |
| DiracDelta($\phi$)       | $\delta(\phi)$                                    | diracdeltafunction.h    |                                                                                                                                                                                                                            |
| exp($\phi$)              | $\mathrm{exp}(\phi)$                              | divide.h                | `exp(ZeroType)` is simplified to `OneType`.                                                                                                                                                                                |
| heaviside($\phi$)        | $\theta(\phi)$                                    | heavisidestepfunction.h | `heaviside(ZeroType)` and `heaviside(OneType)` are simplified to `OneType`.                                                                                                                                                |
| log($\phi$)              | $\log(\phi)$                                      | log.h                   | `log(OneType)` is simplified to `ZeroType`.                                                                                                                                                                                |
| pow<N>($\phi$)           | $\underbrace{\phi\cdot\dots\cdot \phi}_{N-times}$ | pow.h                   | Computes the $\mathtt{N}^{th}$ power of $\phi$ by expanding the multiplication, for performance reasons. `N` must be an integer known at compile time.                                                                     |
| pow($\phi, \chi$)        | $\phi^\chi$                                       | pow.h                   | Computes arbitrary power, $\chi$ can even be a field. `pow($\phi$, ZeroType)` returns `OneType` and `pow(ZeroType, $\phi$)` returns `ZeroType`.                                                                        |
| shift<I>($\phi$)         | `shift<I>($\phi$)$                             | _{\vec n}=\phi          | _{\vec n + \hat I}$                                                                                                                                                                                                        | shift.h         | Shift the object by a unit vector in the $I^{th}$ direction.                                    |
| shift($\phi$,Tag<I>)     | `shift($\phi$, Tag<I>)$                        | _{\vec n}=\phi          | _{\vec n + \hat I}$                                                                                                                                                                                                        | shift.h         | Shift the object by a unit vector in the $I^{th}$ direction. Same as above, different notation. |
| shift<I,J,K,...>($\phi$) | `shift<I,J,...>($\phi$)$                       | _{\vec n}$ $=\phi       | _{(n_1+I,n_2+J,...)}$                                                                                                                                                                                                      | shift.h         | Allows to define an object shifted by an arbitrary vector.                                      |
| sin($\phi$)              | $\sin(\phi)$                                      | sine.h                  | `sin(ZeroType)` returns `ZeroType`.                                                                                                                                                                                        |
| sinh($\phi$)             | $\sinh(\phi)$                                     | sinh.h                  | `sinh(ZeroType)` returns `ZeroType`.                                                                                                                                                                                       |
| sqrt($\phi$)             | $\sqrt{\phi}$                                     | squareroot.h            | Returns `pow($\phi$,0.5`. `sqrt` of `ZeroType` resp. ` OneType` returns `ZeroType` resp. ` OneType`.                                                                                                                     |
| safeSqrt($\phi$)         | $\sqrt{\phi}$                                     | squareroot.h            | Same as `sqrt` except that if $\phi$ is a field, it checks point by point that it is positive and discards results (returns $0$) if not. Useful when taking square roots of fields which fluctuates close to $0$.          |
| tanh($\phi$)             | $\tanh(\phi)$                                     | tanh.h                  | `tanh(ZeroType)` returns `ZeroType`.                                                                                                                                                                                       |

### Complex Scalar Algebra

The files are located in `src/include/TempLat/lattice/algebra/complexalgebra/`.

| Function name                                          | Operation                                                           | File                    | Notes                                                                                               |
| ------------------------------------------------------ | ------------------------------------------------------------------- | ----------------------- | --------------------------------------------------------------------------------------------------- |
| operator+($\varphi_1, \varphi_2$)                      | $\varphi_1 + \varphi_2$                                             | complexfieldadd.h       | Adding real objects to complex objects is supported.                                                |
| operator-($\varphi_1, \varphi_2$)                      | $\varphi_1-\varphi_2$                                               | complexsubtract.h       | Subtracting real objects to complex objects is supported.                                           |
| operator*($\varphi_1, \varphi_2$)                      | $\varphi_1 \varphi_2$                                               | complexmultiply.h       | Multiplication by `ZeroType` returns `ZeroType` and multiplication by `OneType` is simplified away. |
| operator*($\phi, \varphi$), operator*($\varphi, \phi$) | $\phi \varphi, \varphi\phi$                                         | scalarcomplexmultiply.h | Scalar multiplication. $\phi$ is a real expression.                                                 |
| operator/($\varphi, \phi$)                             | $\varphi/ \phi$                                                     | scalarcomplexmultiply.h | Division by a scalar. Implemented through scalar multiplication.                                    |
| asFourier($\varphi$)                                   | Treats a complex object as being a real expression in Fourier space | asfourier.h             |                                                                                                     |
| Complexify($\phi, \chi$)                               | $\phi+i\chi$                                                        | complexwrapper.h        | Makes a complex expressionout of two real expressions.                                              |
| conj($\varphi$)                                        | $\varphi^*$                                                         | complexconjugate.h      | `conj(OneType)` return `OneType` and `conj(ZeroType)` return `ZeroType`                             |
| dagger($\varphi$)                                      | $\varphi^*$                                                         | complexconjugate.h      | Exactly same as `conj`.                                                                             |
| Imag($\varphi$)                                        | $\Im\varphi$                                                        | imag.h                  |                                                                                                     |
| norm2($\varphi$)                                       | $ \Re\varphi^2+\Im\varphi^2$                                        | complexmultiply.h       |                                                                                                     |
| Real($\varphi$)                                        | $\Re\varphi$                                                        | real.h                  |                                                                                                     |
| shift<I>($\varphi$)                                    | `shift<I>($\varphi$)$                                            | _{\vec n}=\varphi       | _{\vec n + \hat I}$                                                                                 | complexshift.h | Shift the object by a unit vector in the $I^{th}$ direction.                                    |
| shift($\varphi$,Tag<I>)                                | `shift($\varphi$, Tag<I>)$                                       | _{\vec n}=\varphi       | _{\vec n + \hat I}$                                                                                 | complexshift.h | Shift the object by a unit vector in the $I^{th}$ direction. Same as above, different notation. |
| shift<I,J,K,...>($\varphi$)                            | `shift<I,J,...>($\varphi$)$                                      | _{\vec n}$ $=\varphi    | _{(n_1+I,n_2+J,...)}$                                                                               | complexshift.h | Allows to define an object shifted by an arbitrary vector.                                      |

### $SU(2)$ Doublet Algebra

The files are located in `src/include/TempLat/lattice/algebra/su2algebra/`.

| Function name                                          | Operation                                                                                                            | File                             | Notes                                                                                                                              |
| ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| operator+($\Phi_1, \Phi_2$)                            | $\Phi_1 + \Phi_2$                                                                                                    | su2doubletsum.h                  |                                                                                                                                    |
| operator-($\Phi_1, \Phi_2$)                            | $\Phi_1-\Phi_2$                                                                                                      | su2doubletsubtract.h             |                                                                                                                                    |
| operator*($\Phi, \varphi$), operator*($\varphi, \Phi$) | $\varphi \Phi$, $\Phi\varphi$                                                                                        | complexfieldsu2doubletmultiply.h | Multiplication of a $SU(2)$-doublet expression by a complex expression. Also support scalar (`double` and `float`) multiplication. |
| MakeSU2Doublet(i, expr)                                | Make an $SU(2) $doublet object out of an expression which depends on `i$\in \{0,1,2,3\}$, which labels the entries. | su2doubletwrapper.h              | `MakeSU2Doublet` is a macro. See the section on macros for more information.                                                       |
| norm2($\Phi$)                                          | $\Phi\cdot\Phi$                                                                                                      | su2doubletdotter.h               |                                                                                                                                    |
| scalar_prod($\Phi_1,\Phi_2$)                           | $\Phi_1\cdot\Phi_2=\varphi_1^{(0)*}\varphi_2^{(0)}+\varphi_1^{(1)*}\varphi_2^{(1)}$                                  | su2doubletdotter.h               | Simplifies scalar product with `ZeroType` to `ZeroType`.                                                                           |
| shift<I>($\Phi$)                                       | `shift<I>($\Phi$)$                                                                                                | _{\vec n}=\Phi                   | _{\vec n + \hat I}$                                                                                                                | su2doubletshift.h | Shift the object by a unit vector in the $I^{th}$ direction.                                    |
| shift($\Phi$,Tag<I>)                                   | `shift($\Phi$, Tag<I>)$                                                                                           | _{\vec n}=\Phi                   | _{\vec n + \hat I}$                                                                                                                | su2doubletshift.h | Shift the object by a unit vector in the $I^{th}$ direction. Same as above, different notation. |
| shift<I,J,K,...>($\Phi$)                               | `shift<I,J,...>($\Phi$)$                                                                                          | _{\vec n}$ $=\Phi                | _{(n_1+I,n_2+J,...)}$                                                                                                              | su2doubletshift.h | Allows to define an object shifted by an arbitrary vector.                                      |

### $SU(2)$ Algebra

The files are located in `src/include/TempLat/lattice/algebra/su2algebra/`.

| Function name         | Operation                                                                                                    | File                       | Notes                                                                                                    |
| --------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------- | -------------------------------------------------------------------------------------------------------- |
| operator+($U_1, U_2$) | $U_1 + U_2$                                                                                                  | su2sum.h                   | Not well defined inside $SU(2)$ group, but can still be useful.                                          |
| operator-($U_1, U_2$) | $U_1 - U_2$                                                                                                  | su2subtract.h              | Not well defined inside $SU(2)$ group, but can still be useful.                                          |
| operator*($U_1, U_2$) | $U_1U_2$                                                                                                     | su2multiply.h              | Matrix $SU(2)$ multiplication.                                                                           |
| operator*($U, \Phi$)  | $U\Phi$                                                                                                      | su2su2doubletmultiply.h    | $SU(2)$ fundamental group action on the doublet.                                                         |
| operator*($\alpha,U$) | $\alpha U$                                                                                                   | scalarsu2doubletmultiply.h | Works only with $\alpha$ a `double` or `float`. Not well defined within the group, but sometimes useful. |
| dagger($U$)           | $U^\dagger$                                                                                                  | su2dagger.h                |                                                                                                          |
| MakeSU2(i, expr)      | Make an $SU(2) $object out of an expression which depends on `i$\in \{0,1,2,3\}$, which labels the entries. | su2wrapper.h               | `MakeSU2` is a macro. See the section on macros for more information.                                    |
| shift<I>($U$)         | `shift<I>($U$)$                                                                                           | _{\vec n}=U                | _{\vec n + \hat I}$                                                                                      | su2shift.h | Shift the object by a unit vector in the $I^{th}$ direction.                                    |
| shift($U$,Tag<I>)     | `shift($U$, Tag<I>)$                                                                                      | _{\vec n}=U                | _{\vec n + \hat I}$                                                                                      | su2shift.h | Shift the object by a unit vector in the $I^{th}$ direction. Same as above, different notation. |
| shift<I,J,K,...>($U$) | `shift<I,J,...>($U$)$                                                                                     | _{\vec n}$ $=U             | _{(n_1+I,n_2+J,...)}$                                                                                    | su2shift.h | Allows to define an object shifted by an arbitrary vector.                                      |
| toSU2($U$)            | Project to group a ''fake" $SU(2)$-like object, like the sum of two $SU(2)$ objects.                         | su2groupwrapper.h          |                                                                                                          |
| trace($U$)            | $\mathrm{Tr}(U)$                                                                                             | su2trace.h                 |                                                                                                          |

### General Purposes Functions and Macros

The files are located in `src/include/TempLat/util/`.

| Function name                       | Operation                                                                                                                                                                                                                                 | File                           |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| ForLoop(i, imin, imax, expr)        | Compile-time for-loop over the `Tag<I>` variable `i`. Varies from `imin` to `imax` included.                                                                                                                                              | rangeiteration/for_in_range.h  |
| MakeArray(i, imin, imax, expr)      | Make a list of `\{expr(imin), expr(imin+1), ..., expr(imax)\`} compatible with the `listoperators` algebra. The resulting list is labeled from `0` to `imax-imin-1`. Useful to make array of expressions made out of fields for instance. | rangeiteration/make_list_tag.h |
| MakeVector(i, imin, imax, expr)     | Make a list of `\{expr(imin), expr(imin+1), ..., expr(imax)\`} compatible with the `listoperators` algebra. The resulting list is labeled from `1` to `imax-imin` and is used to make ''physical" vectors.                                | rangeiteration/make_list_tag.h |
| Total(i, imin, imax, expr)          | Computes the sum `expr(imin)+expr(imin+1)+ ...+ expr(imax)`.                                                                                                                                                                              | rangeiteration/sum_in_range.h  |
| Function(x, expr)                   | Returns a a function of `x` (returns a lambda function whose body is `expr` with variable `x`).                                                                                                                                           | function.h                     |
| IfElse(condition, ifExpr, elseExpr) | Depending on the known-at-compile-time boolean condition `condition`, returns `ifExpr` if `true` or `elseExpr` if `false`.                                                                                                                | staticif.h                     |
| If(condition, ifExpr)               | Depending on the known-at-compile-time boolean condition `condition`, returns `ifExpr` if `true` or `ZeroType` if `false`.                                                                                                                | staticif.h                     |
| IsLess(i, j)                        | Returns a known-at-compile-time boolean, checking whether `i<j` with `i` and `j` `Tag` variables.                                                                                                                                         | rangeiteration/tag.h           |
| IsLessOrEqual(i, j)                 | Returns a known-at-compile-time boolean, checking whether `i<=j` with `i` and `j` `Tag` variables.                                                                                                                                        | rangeiteration/tag.h           |
| IsMore(i, j)                        | Returns a known-at-compile-time boolean, checking whether `i>j` with `i` and `j` `Tag` variables.                                                                                                                                         | rangeiteration/tag.h           |
| IsMoreOrEqual(i, j)                 | Returns a known-at-compile-time boolean, checking whether `i>=j` with `i` and `j` `Tag` variables.                                                                                                                                        | rangeiteration/tag.h           |
| IsEqual(i, j)                       | Returns a known-at-compile-time boolean, checking whether `i==j` with `i` and `j` `Tag` variables.                                                                                                                                        | rangeiteration/tag.h           |
| TempLatVector                       | Overload of the `C++` `std::vector` compatible with our vector algebra. Also has `operator()` overloaded to access its component, to match the syntax of the other objects in the library.                                                | templatvector.h                |
| TempLatArray                        | Overload of the `C++` `std::array` compatible with our vector algebra. Also has `operator()` overloaded to access its component, to match the syntax of the other objects in the library.                                                 | templatarray.h                 |

### Accessing into a given location

CosmoLattice is designed to work at an abstract level; all iterations over the lattice are handled internally. However, in some specific circumstances, one may need to *exceptionally* retrieve a given point on the lattice. This is possible by calling the function `operator()(bool\& test, vector<ptrdiff_t> position)` defined in `src/include/TempLat/lattice/field/views/fieldviewconfig.h` (for coordinate space) and `src/include/TempLat/lattice/field/views/fieldviewfourier.h`  (for Fourier space). The first argument is a boolean, which is set to `false` by the function on all processors, except the one that is host to the requested coordinates (when using parallelization, not all processes have access to all coordinates, see Sec. [*Parallelization*][subsec_para] for more details). The second argument is the coordinate we want to access, passed as an array. The coordinates run from $n_i = -N/2+1$ to $n_i = N/2$ for all directions in coordinate space $i = 1,2,3$ and from $\tilde n_i = -N/2+1$ to $\tilde n_i = N/2$ for $i = 1,2$ directions except for $i = 3$, for which it runs only between $\tilde n_3 = 0$ and $\tilde n_3 =  N/2-1$ in Fourier space.

Similarly, one can set the value of a field at a given location by calling a `set(value, position)` function, where value is the value one wants to set the field to at position `position`. Value is expected to be a number/complex number in configuration/Fourier space.

*Example in configuration space:*
```cpp
phi.set(5.0, {-1,2,3}); //set the field phi to 5 at position (-1,2,3)

bool test;
double res = phi(test, {-1,2,3}) //reads the value of the field phi at position (-1,2,3)
if(test) std::cout << res << std::endl; //prints the above value of the field
```

Assuming `phi` is a real scalar field, this will first set the field `phi` to $5$ at position $(-1,2,3)$. It then retrieve this value and store it in `res`. If run in parallel, only one processor knows about this value and this processor is asked to print it on the last line.

*Example in Fourier space:*
```cpp
phi.inFourierSpace().set(std::complex<double>(5.0, 10.0), {1,-2,3}); //set the field phi to 5 + I10 at position (1,-2,3)

bool test;
std::complex<double> var = phi.inFourierSpace()(test, {1,-2,3}) //reads the value of the Fourier amplitude of phi at coordinate (1,-2,3) of the reciprocal lattice
if(test) std::cout << var.real() << " + I" << var.imag() << std::endl; //prints the above Fourier amplitude of the field at coordinate (1,-2,3) of the reciprocal lattice
```

This will set the Fourier amplitude of the field `phi` to $5+I10$ at coordinate $(1,-2,3)$ of the reciprocal lattice. It then retrieves this value and stores it in `var`. If run in parallel, only one processor knows about this value and this processor is asked to print it on the last line.

